# Alpha 1.0.8_01

Release-gate hotfix for the Alpha 1.0.8 line.

## Included

- Fixes a UI contract violation that broke the `build-and-publish` workflow (`border-left` structural width drifted to `2px`).
- Restores the required 1px structural border width in mail message-row styling.
- Revalidates all release gates (Go tests, Rust workspace tests, and UI contract/surface checks).
