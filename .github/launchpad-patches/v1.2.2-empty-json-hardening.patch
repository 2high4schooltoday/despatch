diff --git a/rust/crates/launchpad-cli/src/main.rs b/rust/crates/launchpad-cli/src/main.rs
index 0167bac..0698217 100644
--- a/rust/crates/launchpad-cli/src/main.rs
+++ b/rust/crates/launchpad-cli/src/main.rs
@@ -5864,9 +5864,13 @@ fn read_operation_receipt_from_execution(
     let Some(path) = execution.receipt_path.as_deref() else {
         return Ok(None);
     };
-    let receipt = serde_json::from_slice(
-        &fs::read(path).with_context(|| format!("failed to read operation receipt at {path}"))?,
-    )?;
+    let bytes =
+        fs::read(path).with_context(|| format!("failed to read operation receipt at {path}"))?;
+    if bytes.iter().all(|byte| byte.is_ascii_whitespace()) {
+        return Ok(None);
+    }
+    let receipt = serde_json::from_slice(&bytes)
+        .with_context(|| format!("failed to parse operation receipt at {path}"))?;
     Ok(Some(receipt))
 }
 
@@ -14558,13 +14562,17 @@ fn seed_operation_inputs_from_receipts(
         if !receipt_path.exists() {
             continue;
         }
-        let receipt: lp_runtime::OperationReceipt =
-            serde_json::from_slice(&fs::read(&receipt_path).with_context(|| {
-                format!(
-                    "failed to read operation receipt at {}",
-                    receipt_path.display()
-                )
-            })?)?;
+        let bytes = fs::read(&receipt_path).with_context(|| {
+            format!(
+                "failed to read operation receipt at {}",
+                receipt_path.display()
+            )
+        })?;
+        if bytes.iter().all(|byte| byte.is_ascii_whitespace()) {
+            continue;
+        }
+        let receipt: lp_runtime::OperationReceipt = serde_json::from_slice(&bytes)
+            .with_context(|| format!("failed to parse operation receipt at {}", receipt_path.display()))?;
         if let Some(value) = receipt.outputs.get(&input.name).cloned() {
             values.insert(input.name.clone(), value);
         }
diff --git a/rust/crates/lp_project/src/lib.rs b/rust/crates/lp_project/src/lib.rs
index 741114a..7551901 100644
--- a/rust/crates/lp_project/src/lib.rs
+++ b/rust/crates/lp_project/src/lib.rs
@@ -215,9 +215,13 @@ impl ProjectLayout {
         if !self.lockfile_path.exists() {
             return Ok(Lockfile::default());
         }
-        Ok(serde_json::from_str(&fs::read_to_string(
-            &self.lockfile_path,
-        )?)?)
+        let content = fs::read_to_string(&self.lockfile_path)
+            .with_context(|| format!("failed to read lockfile `{}`", self.lockfile_path.display()))?;
+        if content.trim().is_empty() {
+            return Ok(Lockfile::default());
+        }
+        serde_json::from_str(&content)
+            .with_context(|| format!("failed to parse lockfile `{}`", self.lockfile_path.display()))
     }
 
     pub fn persist_lockfile(&self, lockfile: &Lockfile) -> Result<()> {
diff --git a/rust/crates/lp_runtime/src/lib.rs b/rust/crates/lp_runtime/src/lib.rs
index 0a58f2a..0436754 100644
--- a/rust/crates/lp_runtime/src/lib.rs
+++ b/rust/crates/lp_runtime/src/lib.rs
@@ -7,7 +7,7 @@ use std::sync::{Mutex, OnceLock};
 use std::thread;
 use std::time::{Duration, Instant, SystemTime, UNIX_EPOCH};
 
-use anyhow::{Result, anyhow};
+use anyhow::{Context, Result, anyhow};
 use lp_ir::{
     Action, ActionTarget, BinaryOp, CallTarget, CastTarget, Condition, Expr, InterpolatedPart,
     Literal, MapEntry, MatchPattern, Operation, Output, Program, StepKind, UnaryOp,
@@ -5831,7 +5831,13 @@ fn load_pending_resume_state(workdir: &Path, operation: &str) -> Result<Option<R
     candidates.sort();
     candidates.reverse();
     for path in candidates {
-        let state: ResumeState = read_json_file(&path)?;
+        let bytes = fs::read(&path)
+            .with_context(|| format!("failed to read pending resume state `{}`", path.display()))?;
+        if bytes.iter().all(|byte| byte.is_ascii_whitespace()) {
+            continue;
+        }
+        let state: ResumeState = serde_json::from_slice(&bytes)
+            .with_context(|| format!("failed to parse pending resume state `{}`", path.display()))?;
         if state.operation == operation {
             return Ok(Some(state));
         }
@@ -5872,7 +5878,10 @@ fn write_json_file<T: Serialize>(path: &Path, value: &T) -> Result<()> {
 }
 
 fn read_json_file<T: DeserializeOwned>(path: &Path) -> Result<T> {
-    Ok(serde_json::from_slice(&fs::read(path)?)?)
+    let bytes =
+        fs::read(path).with_context(|| format!("failed to read json file `{}`", path.display()))?;
+    serde_json::from_slice(&bytes)
+        .with_context(|| format!("failed to parse json file `{}`", path.display()))
 }
 
 fn detect_hostname() -> String {
diff --git a/rust/crates/lp_update/src/lib.rs b/rust/crates/lp_update/src/lib.rs
index 77a302e..01cd669 100644
--- a/rust/crates/lp_update/src/lib.rs
+++ b/rust/crates/lp_update/src/lib.rs
@@ -338,8 +338,13 @@ pub fn load_preferences_for(app: &UpdateApp) -> Result<UpdatePreferences> {
     if !path.exists() {
         return Ok(UpdatePreferences::default());
     }
-    let bytes = fs::read(path)?;
-    Ok(serde_json::from_slice(&bytes)?)
+    let bytes = fs::read(&path)
+        .with_context(|| format!("failed to read update preferences `{}`", path.display()))?;
+    if bytes.iter().all(|byte| byte.is_ascii_whitespace()) {
+        return Ok(UpdatePreferences::default());
+    }
+    serde_json::from_slice(&bytes)
+        .with_context(|| format!("failed to parse update preferences `{}`", path.display()))
 }
 
 pub fn save_preferences(preferences: &UpdatePreferences) -> Result<()> {
@@ -359,8 +364,13 @@ pub fn load_status_for(app: &UpdateApp) -> Result<UpdateStatus> {
     if !path.exists() {
         return Ok(UpdateStatus::empty_for(app));
     }
-    let bytes = fs::read(path)?;
-    let mut status: UpdateStatus = serde_json::from_slice(&bytes)?;
+    let bytes =
+        fs::read(&path).with_context(|| format!("failed to read update status `{}`", path.display()))?;
+    if bytes.iter().all(|byte| byte.is_ascii_whitespace()) {
+        return Ok(UpdateStatus::empty_for(app));
+    }
+    let mut status: UpdateStatus = serde_json::from_slice(&bytes)
+        .with_context(|| format!("failed to parse update status `{}`", path.display()))?;
     status.current_version = app.current_version.to_string();
     Ok(status)
 }
@@ -374,8 +384,13 @@ pub fn load_history_for(app: &UpdateApp) -> Result<Vec<UpdateHistoryEntry>> {
     if !path.exists() {
         return Ok(Vec::new());
     }
-    let bytes = fs::read(path)?;
-    Ok(serde_json::from_slice(&bytes)?)
+    let bytes = fs::read(&path)
+        .with_context(|| format!("failed to read update history `{}`", path.display()))?;
+    if bytes.iter().all(|byte| byte.is_ascii_whitespace()) {
+        return Ok(Vec::new());
+    }
+    serde_json::from_slice(&bytes)
+        .with_context(|| format!("failed to parse update history `{}`", path.display()))
 }
 
 pub fn load_release_notes(status: &UpdateStatus) -> Result<Option<String>> {
